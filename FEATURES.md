# Daily CAP: A Comprehensive Technical Feature Catalog

Daily CAP is a Canadian federal digital service quality benchmarking system that automates accessibility, performance, and usability scanning across high-traffic Government of Canada entry points. The project includes a complete pipeline for data ingestion, parallel scanning, impact aggregation, and accessible HTML report generation.

## Core Architecture

The system follows a linear workflow: traffic data -> parallel scanning -> metric aggregation -> static report publishing. It processes approximately 100 top-traffic URLs daily through Lighthouse and axe-core scanners, extracting structured findings mapped to WCAG 2.2 requirements and Canadian bilingual obligations. English and French URLs are treated as separate scan targets throughout the pipeline.

## Key Components

**Configuration Management**: Central configuration module (`src/config/defaults.js`) controls scan parameters, URL limits, concurrency, and multi-context scan targets. Defaults can be overridden via CLI flags.

**Traffic Data Ingestion**: Flexible adapter pattern accepts data from Canada.ca recent activity pages, Top Tasks lists, or static seed files. Multiple ingest sources are orchestrated through `src/ingest/orchestrate.js`. Falls back to seed data when live ingestion fails.

**Bilingual Inventory Building**: `src/inventory/build-inventory.js` normalizes and deduplicates sources into a ranked inventory with `url_en` and `url_fr` fields where available. `src/inventory/ranking-engine.js` applies deterministic, tier-based ranking logic. Bilingual pairs are tracked through the full pipeline.

**Parallel Scanning**: `src/scanners/execution-manager.js` orchestrates concurrent Lighthouse and axe-core scans with configurable concurrency (default: 2), timeouts, and retry logic. Scans run across four contexts: desktop/mobile x light/dark.

**Impact Metrics**: `src/aggregation/impact-model.js` computes traffic-weighted quality impact using Statistics Canada disability prevalence data. Estimates directional impact on Canadians with disabilities based on automated barrier signals. All estimates are explicitly labeled as directional.

**HTML Report Generation**: All pages use semantic HTML5 landmarks, accessible tables with scoped headers, ARIA labels, skip-to-content links, and dark/light mode toggle with `localStorage` persistence. Generated by `src/publish/render-pages.js`.

## Accessibility and Standards

Generated reports target WCAG 2.2 Level AA. The system produces bilingual parity analysis comparing English and French service quality. Findings are mapped to WCAG success criteria. Each report includes policy context explaining why barriers matter to Canadians accessing federal services.

## Bilingual Analysis

Bilingual support is a first-class pipeline feature, not a post-processing step.

- Inventory records carry `url_en` and `url_fr` fields.
- English and French URLs are scanned as separate targets.
- `src/aggregation/bilingual-parity.js` computes EN/FR score gaps and flags missing counterparts.
- Parity gaps are reported explicitly rather than silently averaged.
- Missing language counterparts are surfaced as a distinct finding type.

## Accessibility Statement Detection

`src/scanners/accessibility-statement-runner.js` and `src/aggregation/accessibility-statements.js` detect and assess accessibility statements on scanned pages.

Checks include:

- whether a statement exists
- whether it is linked clearly from the page
- whether contact information is present
- whether compliance status is mentioned
- whether alternatives or support paths are mentioned
- whether a date or freshness marker is present
- whether English and French variants both exist

Statement presence is reported as a governance and transparency signal. It does not indicate accessibility conformance.

## Platform and CMS Detection

`src/scanners/platform-fingerprint-runner.js` and `src/aggregation/platform-signals.js` detect CMS, design system, and hosting platform signals from scanned pages. Results feed into CMS and platform cohort breakdowns in reports.

## Institution Scorecards

`src/aggregation/institution-scorecards.js` groups scanned URLs by Government of Canada institution. Each institution receives an aggregated scorecard. `src/aggregation/institution-trends.js` maintains per-institution trend history. Individual institution pages are published at `docs/reports/daily/YYYY-MM-DD/details/institutions/`.

## Barrier History Tracking

`src/aggregation/barrier-history.js` maintains a rolling 30-day view of key barrier signals across daily runs:

- missing French counterparts
- missing or low-quality accessibility statements
- high EN/FR accessibility score gaps

Results are rendered as trend charts in each daily report.

## Trend Analysis and Regression Alerts

`src/aggregation/trend-analysis.js` compares the current run against the previous report. Day-over-day regressions on key metrics trigger alerts in the published report. Historical data spans up to 30 days.

## Priority Issue Tracking

`src/aggregation/priority-issues.js` identifies and ranks the highest-impact recurring accessibility barriers across scanned URLs. Issues are prioritized by estimated reach and severity.

## Cohort Quality Analysis

`src/aggregation/cohort-quality.js` computes quality metrics by service-pattern cohort (sign-in, forms, dashboards, etc.) and by CMS or platform type.

## Operational Automation

GitHub Actions workflows handle daily scans with automatic report publishing to GitHub Pages. Reports older than 14 days are archived by `src/publish/archive-reports.js` while retaining core JSON data.

## Project Structure

```
run.js                          Main pipeline orchestrator
src/config/defaults.js          Scan parameters and defaults
src/ingest/
  canada-recent-activity.js     Canada.ca recent activity ingest
  canada-seed.js                Static seed inventory
  curated-endpoints.js          Curated service endpoint list
  discovery-cohort.js           Discovery cohort builder
  top-tasks.js                  Top Tasks ingest
  orchestrate.js                Multi-source ingest orchestrator
src/inventory/
  build-inventory.js            Normalizes sources into ranked inventory
  ranking-engine.js             Deterministic tiered ranking
  schema.js                     Inventory record schema
  tier-config.js                Tier definitions and weights
src/scanners/
  execution-manager.js          Parallel scan orchestrator
  lighthouse-runner.js          Lighthouse multi-context runner
  axe-runner.js                 axe-core structured findings extractor
  accessibility-statement-runner.js  Statement detection scanner
  platform-fingerprint-runner.js     CMS/platform signal detector
  scangov-runner.js             ScanGov scanner adapter
src/aggregation/
  build-report.js               Assembles full daily report payload
  score-aggregation.js          Score rollups and weighted averages
  bilingual-parity.js           EN/FR parity analysis
  accessibility-statements.js   Statement quality aggregation
  impact-model.js               Directional impact estimation
  barrier-history.js            Rolling barrier trend tracker
  trend-analysis.js             Day-over-day comparison and alerts
  cohort-quality.js             Service-pattern cohort analysis
  institution-scorecards.js     Per-institution aggregation
  institution-trends.js         Per-institution history
  lighthouse-context-analysis.js  Multi-context Lighthouse results
  platform-signals.js           CMS/platform cohort aggregation
  priority-issues.js            High-impact issue identification
src/enrichment/                 Reserved for enrichment modules
src/publish/
  publish-report.js             Writes output files and manifests
  render-pages.js               HTML report rendering
  archive-reports.js            Archives reports older than 14 days
src/utils/
  date.js                       Date parsing and resolution
  fetch-with-timeout.js         HTTP fetch with timeout
  fs.js                         File system helpers
```

## Command-Line Interface

Main pipeline (`run.js`):

```bash
node run.js --date YYYY-MM-DD --limit N --mode live|mock --output-root PATH --source-mode live|seed
```

| Flag | Default | Description |
| :--- | :--- | :--- |
| `--date` | Today | Run date in YYYY-MM-DD format |
| `--limit` | 130 | Maximum URLs to scan |
| `--mode` | mock | `live` runs real scans; `mock` uses fixture data |
| `--output-root` | `./` | Root directory for output |
| `--source-mode` | live | `live` ingests from Canada.ca; `seed` uses static seed |

## Output Artifacts

```
docs/reports/daily/YYYY-MM-DD/
  index.html                    Daily report page
  report.json                   Full report data payload
  details/
    *.json                      Per-URL detail records
    institution-trends.html     Institution trends overview
    institutions/
      *.html                    Per-institution scorecard pages
docs/reports/
  index.html                    Report dashboard and history index
```

Reports older than 14 days are archived as zip files. Core `report.json` files are retained for historical trend computation.

## Testing Infrastructure

Tests live in `test/` and run with `node --test`.

| Test file | Coverage area |
| :--- | :--- |
| `accessibility-statements.test.js` | Statement detection logic |
| `archive-reports.test.js` | Archival and retention |
| `axe-runner.test.js` | axe-core extraction |
| `barrier-history.test.js` | Rolling barrier trend |
| `bilingual-parity.test.js` | EN/FR parity analysis |
| `build-report.integration.test.js` | Full pipeline integration |
| `cohort-quality.test.js` | Cohort aggregation |
| `fetch-with-timeout.test.js` | HTTP utility |
| `impact-model.test.js` | Directional impact estimation |
| `institution-scorecards.test.js` | Institution aggregation |
| `lighthouse-context-analysis.test.js` | Multi-context analysis |
| `lighthouse-runner.test.js` | Lighthouse runner |
| `priority-issues.test.js` | Issue prioritization |
| `publish-report.test.js` | Output publishing |
| `ranking-engine.test.js` | Inventory ranking |
| `trend-analysis.test.js` | Trend and regression |

Run tests: `npm test`

## HTML Report Features

### Daily report page sections

- **Header**: Run date, scan summary, bilingual coverage count
- **Bilingual parity**: EN/FR pair coverage, parity gap distribution, missing counterparts
- **Accessibility impact**: Traffic-weighted barrier estimates by disability category using Statistics Canada prevalence rates
- **Accessibility statements**: Coverage rate, quality signal breakdown, EN/FR statement parity
- **Performance summary**: Core Web Vitals signals, mobile vs. desktop comparison
- **Top URLs**: Ranked table with per-URL scores, language, institution, trend arrows
- **Detailed findings**: Expandable axe-core findings with WCAG mapping
- **Platform and CMS breakdown**: Distribution by detected CMS and design system
- **Institution scorecards**: Per-institution aggregated scores with trend indicators
- **Barrier history chart**: 30-day rolling view of key barrier signals
- **Priority issues**: Highest-reach recurring barriers
- **Trend charts**: Historical score trends across the 30-day window
- **Methodology notes**: Explicit caveats on automated signal limitations

### Dashboard page

- Report history index with date links
- Download links for archived reports (zip)
- Latest aggregated scores

### Self-compliance

Daily CAP reports are tested for WCAG 2.2 AA conformance. The project benchmarks accessibility; its own outputs must meet or exceed the same standards it measures.

## Accessibility Compliance Features

| Feature | Status |
| :--- | :--- |
| WCAG 2.2 Level AA target | Required |
| Semantic HTML5 landmarks | Implemented |
| Proper heading hierarchy | Implemented |
| Accessible table headers | Implemented |
| ARIA labels on ambiguous elements | Implemented |
| Skip-to-content link | Implemented |
| Dark/light mode toggle | Implemented |
| `prefers-color-scheme` media query | Implemented |
| `prefers-reduced-motion` media query | Implemented |
| `escapeHtml()` on all external data | Implemented |
| Keyboard navigation | Implemented |
| No color-only information | Implemented |

## Methodology Guardrails

Daily CAP reports use precise language to avoid overclaiming:

| Avoid | Use instead |
| :--- | :--- |
| "certified accessible" | "automated signal" |
| "measured affected users" | "directional estimate" |
| "all Government of Canada services" | "benchmark coverage" |
| "compliant statement" | "detected statement" |
| "proven barrier-free" | "no automated barriers detected" |

## Adapting for Other Jurisdictions

Daily CAP is designed for the Canadian federal context. Key adaptation points for other jurisdictions:

- **Traffic data source**: Replace Canada.ca recent activity with the local analytics source
- **Disability prevalence data**: Replace Statistics Canada data in `src/aggregation/impact-model.js`
- **Accessibility standards mapping**: Update WCAG and bilingual requirements as appropriate
- **Domain registry**: Update institution detection logic in `src/aggregation/institution-scorecards.js`
- **Bilingual requirements**: Adjust or disable if the jurisdiction is not officially bilingual
- **Seed inventory**: Replace `src/ingest/canada-seed.js` with a jurisdiction-specific seed list

## Dependencies and Requirements

- **Runtime**: Node.js >= 24
- **Key packages**: `@playwright/test`, `@axe-core/playwright`
- **Environment**: Google Chrome must be available for live scan mode
- **Install**: `npm install`
- **Quick start (mock mode)**: `node run.js --date 2026-03-25 --limit 25 --mode mock`
