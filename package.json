{
  "name": "daily-cap",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Daily CAP baseline pipeline for Canadian analytics benchmarking",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "run": "node run.js",
    "test": "node --test"
  },
  "dependencies": {
    "@axe-core/playwright": "^4.11.1",
    "@playwright/test": "^1.59.1",
    "js-yaml": "^4.1.1"
  }
}
